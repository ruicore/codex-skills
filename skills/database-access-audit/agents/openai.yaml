interface:
  display_name: "Database Access Audit"
  short_description: "Audit database access patterns"
  default_prompt: "Use $database-access-audit to perform a read-only audit of database access patterns and produce prioritized findings."
