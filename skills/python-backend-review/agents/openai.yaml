interface:
  display_name: "Python Backend Review"
  short_description: "Python backend version review"
  default_prompt: "Use $python-backend-review to review this Python backend repository against a target Python version and Python backend fundamentals standard, producing prioritized findings that have survived an explicit counter-evidence / disproof pass."
