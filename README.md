# Codex Skills

Personal Codex skill definitions and supporting metadata.

## Layout

- `skills/`: Codex skill directories.
- `skills/*/SKILL.md`: agent-facing workflow instructions.
- `skills/*/agents/openai.yaml`: optional metadata for skill discovery and UI presentation.

The repository root is intentionally kept small so other Codex-related assets can be added beside `skills/` later.
